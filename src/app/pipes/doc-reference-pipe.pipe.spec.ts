import { DocReferencePipePipe } from './doc-reference-pipe.pipe';

describe('DocReferencePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DocReferencePipePipe(null);
    expect(pipe).toBeTruthy();
  });
});
